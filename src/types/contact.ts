export interface ContactFormValues {
  name: string;
  phone: string;
  email: string;
  message: string;
}

export interface Testimonial {
  name: string;
  role: string;
  text: string;
  img: string;
}
